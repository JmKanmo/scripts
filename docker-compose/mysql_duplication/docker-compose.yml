version: "3"
services:
  db-master:
    build: 
      context: ./
      dockerfile: master/Dockerfile
    restart: always
    environment:
      MYSQL_DATABASE: mysql_test_master_db
      MYSQL_USER: junmokang
      MYSQL_PASSWORD: wnsah12
      MYSQL_ROOT_PASSWORD: wnsah12
    ports:
      - 3400:3306
    # Where our data will be persisted
    volumes:
      - ./master/mysql/data/:/var/lib/mysql
      - ./master/mysql-files/:/var/lib/mysql-files
    networks:
      - net-mysql
  
  db-slave:
    build: 
      context: ./
      dockerfile: slave/Dockerfile
    restart: always
    environment:
      MYSQL_DATABASE: mysql_test_slave_db
      MYSQL_USER: junmokang
      MYSQL_PASSWORD: wnsah12
      MYSQL_ROOT_PASSWORD: wnsah12
    ports:
      - 3401:3306
    # Where our data will be persisted
    volumes:
      - ./slave/mysql/data/:/var/lib/mysql
      - ./slave/mysql-files/:/var/lib/mysql-files
    networks:
      - net-mysql
  
networks: 
  net-mysql:
    driver: bridge